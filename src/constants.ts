export enum IMAGE_DIMENSIONS {
  width = 800,
  height = 400,
}

export const SQUARE_DIMENSION = 8;
export const MAX_PIXELS_FOR_SUBMISSION = 20;
export const FINAL_SUBMISSION_TIMER_MINUTES = 5;
export const SUBMISSION_TIMEOUT_IN_SECONDS = 15;
